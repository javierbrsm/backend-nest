pipeline {
    agent any

    stages {
        stage('Prueba') {
            steps {
                sh 'echo "hola a todos desde el pipeline"'
                sh 'echo "saliendo del pipeline"'
            }
        }
    }
}
